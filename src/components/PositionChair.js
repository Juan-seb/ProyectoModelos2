import styled from "styled-components"

const StylesPositionChair = styled.div`
    width: 45px;
    height: 45px;
    border: thin solid gray;
    margin: .3rem;
`;

const PositionChair = ({ row, position }) => {
    return (
        <StylesPositionChair>

        </StylesPositionChair>
    )
}

export default PositionChair
